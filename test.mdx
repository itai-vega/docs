---
mode: "wide"
---

export const CustomCodeBlock = ({ code }) => (
  <pre>
    <code>{code}</code>
  </pre>
);

---

## title: Reference Table
description: "Our Reference Table"

| Function / Operator | Description                             | Query Syntax |
| ------------------- | --------------------------------------- | ------------ |
| `where`             | Filters rows based on a condition.      |              |
| `project`           | Selects specified fields.               |              |
| `==`                | Checks for equality.                    |              |
| `!=`                | Checks for inequality.                  |              |
| `>`                 | Checks if greater than.                 |              |
| `>=`                | Checks if greater than or equal to.     |              |
| `<`                 | Checks if less than.                    |              |
| `<=`                | Checks if less than or equal to.        |              |
| `and`               | Combines multiple true conditions.      |              |
| `or`                | Combines multiple conditions; one true. |              |